# 포트폴리오 사이트

게이머를 위한 개발자 포트폴리오 웹사이트입니다.

## 기술 스택

- React 18
- Vite
- CSS3
- Custom Hooks
- Error Boundaries
- PWA (Service Worker)
- Vitest (Testing)
- Web Vitals

## 프로젝트 구조

```
forgamersbygamers/
├── src/
│   ├── components/          # React 컴포넌트
│   │   ├── ThemeToggle.jsx
│   │   ├── LanguageToggle.jsx
│   │   ├── ShareButton.jsx
│   │   ├── ImageGallery.jsx
│   │   ├── Toast.jsx
│   │   └── ErrorBoundary.jsx
│   ├── hooks/               # 커스텀 훅
│   │   ├── useTheme.js
│   │   ├── useLanguage.js
│   │   ├── useTypingText.js
│   │   └── useSEO.js
│   ├── utils/               # 유틸리티
│   │   ├── translations.js
│   │   └── constants.js
│   ├── styles/              # 스타일
│   │   ├── variables.css
│   │   └── animations.css
│   ├── App.jsx              # 메인 앱 컴포넌트
│   ├── App.css
│   ├── main.jsx             # 진입점
│   └── index.css             # 전역 스타일
├── public/
│   ├── images/
│   ├── fonts/
│   ├── robots.txt
│   └── sitemap.xml
├── index.html
├── package.json
├── vite.config.js
└── vercel.json               # Vercel 배포 설정
```

## 주요 기능

- 🌍 **다국어 지원**: 영어, 한국어, 중국어
- 🌓 **다크/라이트 모드**: 시스템 설정 자동 감지
- 🖼️ **인터랙티브 이미지 갤러리**: 드래그, 스와이프, 휠 지원
- ♿ **접근성**: ARIA 레이블, 키보드 네비게이션
- 🚀 **성능 최적화**: React.memo, 이미지 lazy loading, 코드 스플리팅
- 🔍 **SEO 최적화**: 메타 태그, Open Graph, sitemap, 구조화된 데이터 (JSON-LD)
- 🛡️ **에러 처리**: Error Boundary
- 📱 **PWA 지원**: 오프라인 작동, 앱 설치 가능
- 📊 **Analytics 준비**: 이벤트 추적 시스템
- ⚡ **Web Vitals 모니터링**: 성능 메트릭 자동 추적
- ⌨️ **키보드 단축키**: `?` 키로 도움말 표시
- 📴 **오프라인 페이지**: 네트워크 끊김 시 안내
- 📊 **스크롤 인디케이터**: 페이지 진행도 표시
- 🧪 **테스트**: Vitest + React Testing Library
- 🌐 **언어별 라우팅**: `/en`, `/ko`, `/zh` URL 지원
- 💀 **로딩 스켈레톤**: 초기 로딩 시 스켈레톤 UI

## 설치 및 실행

### 로컬 개발 환경 설정

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev
```

개발 서버는 기본적으로 `http://localhost:5173`에서 실행됩니다.

### 빌드

```bash
# 프로덕션 빌드
npm run build

# 빌드 미리보기
npm run preview
```

## 배포

### Vercel 배포

1. GitHub에 프로젝트를 푸시합니다.
2. [Vercel](https://vercel.com)에 로그인합니다.
3. "New Project"를 클릭하고 GitHub 저장소를 선택합니다.
4. Vercel이 자동으로 설정을 감지하고 배포합니다.

또는 Vercel CLI를 사용할 수 있습니다:

```bash
npm i -g vercel
vercel
```

## 커스터마이징

- `src/utils/translations.js`: 번역 텍스트 수정
- `src/components/`: 컴포넌트 수정
- `src/styles/variables.css`: CSS 변수로 색상 및 스타일 커스터마이징
- `public/images/`: 이미지 교체
- `public/manifest.json`: PWA 설정 수정
- `src/utils/analytics.js`: Analytics 설정 (Google Analytics 등)

## 개발 도구

### 코드 포맷팅
```bash
npm run format
```

### 린팅
```bash
npm run lint
```

### 테스트
```bash
# 테스트 실행
npm run test

# 테스트 UI 모드
npm run test:ui

# 커버리지 포함 테스트
npm run test:coverage
```

## 키보드 단축키

- `L`: 언어 변경
- `T`: 테마 변경
- `←/→`: 이미지 넘기기
- `ESC`: 확대된 이미지 닫기
- `?`: 키보드 단축키 도움말 표시/숨기기

## 라이선스

MIT
